extends layout

block content
  script.
    function load(){
      console.log("cared")
    }
  style.
    a{
      height:30px;
      font-size:12px !important;
      margin:4px;
    }
    .item{
      position:fixed;
      bottom:0;
      z-index:-100
    }
    .img1{
      right:30;
    }
    .img2{
      right:30px;

    }
    body{
      //- background:linear-gradient(rgba(0,0,0,0.7),rgba(0,0,0,0.7)),url(https://images.pexels.com/photos/257552/pexels-photo-257552.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=2);
      background:#eab676;
      
      height:100vh;
      background-repeat:no-repeat;
    }
    p{
      text-align:center;
      width:100%;
    }
    .row{
      display:flex;
      justify-content:center
    }
    h3{
      text-align:center;
      width :100%;
    }
    h1{
      color:white;
      text-shadow:1px 1px 12px black;
    }
    #buttons{
      border-radius:12px;
      width:100%;
      display:flex;
      flex-wrap:wrap;
      justify-content:space-evenly
    }
    .center{
      text-align:center;
      position:relative;
      top:50%;
      transform:translateY(-50%);
    }

  .center
    h1= title
    a(class='btn btn-primary' href=`/insects/create`) Create new Insect Instance
    .container-fluid(onload='load()')
      .row 
        each insect,key in results
          div(class="col-sm-2" style=`margin:5px;border-radius:12px;text-align:center;background:white;border:solid 2px black;display:flex;flex-direction:row;flex-wrap:wrap` onclick=`buy(${key})`) 
              h3= insect.insect_Name
              div#buttons
                a(class='btn btn-primary' href=`/insects/detail/?id=${insect._id}`) Details
                a(class='btn btn-primary' href=`/insects/update/?id=${insect._id}`) Update
                a(class='btn btn-primary' href=`/insects/delete/?id=${insect._id}`) Delete
  img(src="/images/item2.png",class="item img1" height="60" width='100' alt="alt")
  img(src="/images/item3.png",class="item img2" height="90" width='105' alt="alt")
  script.
    function load(){
      console.log("cared")
    }
    function buy(index){
      alert("You purchased item "+index)
    }

